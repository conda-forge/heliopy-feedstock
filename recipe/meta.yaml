{% set name = "HelioPy" %}
{% set version = "0.13.0" %}
{% set sha256 = "f23361b7d8240f59a6229f53adb6077028fd2f2cf7f2265cc1d0b4286069fd5b" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 1
  script: {{ PYTHON }} -m pip install . --no-deps -vv
  noarch: python

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - numpy
    - astropy
test:
  imports:
    - heliopy

about:
  home: http://heliopy.org/
  license: GPL-3.0-only
  license_family: GPL
  license_file: LICENSE
  summary: Python for heliospheric physics
  description: |
    Tools to work with 1D in-situ timeseries measurements of space plasmas.
  doc_url: http://docs.heliopy.org/en/stable/
  dev_url: https://github.com/heliopython/heliopy

extra:
  recipe-maintainers:
    - dstansby
